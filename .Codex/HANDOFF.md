# 最新交付：2026-09-25 本机设备控制

- 最新追加：修复设备工具记录展开时超长单行 JSON 导致 Compose Constraints 越界闪退，纯合成数据先复现后通过回归；原始复制内容保持完整。仅阅读实机日志元数据/堆栈，未读正文。设备控制/搜索服务统一卡片栏目，连接状态就近展示，模型测试结果移至测试按钮下。详见 [记录](memory/2026-09-25-tool-record-crash-and-settings.md)。

- 追加：实机已补装 Shizuku 13.6.0；网络搜索服务、设备控制入口合并到“设置 → 搜索与设备”，子页返回设置。设备控制新增官方下载入口。最新 UAH 覆盖安装保留数据成功；构建/lint 与模拟器导航检查通过。最新 APK SHA256 `B889A7E41373E4F4CE5504291C86428F5B61B8FD04051471B8FEBD54E6EAC9DA`，下方 APK 及入口说明为前一版记录。

- 无障碍 + Shizuku 第一版已接入，详见 [实现、验证与用户测试说明](memory/2026-09-25-device-control.md)。Herdr 中 agy 保持用户指定默认模型。
- 构建、65 项针对性 JVM、lint、4 项真实模拟器集成测试通过；全套 JVM 332 项中有 1 项既有 Windows 符号链接测试失败，未修复。模拟器 API37，实机 API36 待用户验收。
- NX809J 安装因签名冲突按既有授权卸载重装 Success，旧版本地数据已清除；没有操作手机界面。APK SHA256 `3BC7DC0B2F6A3DF6859254227BB6955C9CC2D3DAF84CC5F9D661E54A2AA1D7DA`。
- 手机需重新配置模型，从抽屉“设备控制”开启开关、无障碍；Shizuku 需用户安装启动后授权连接。Auto 可连续执行，Accept Edit 通过悬浮面板逐次审批，通知/悬浮按钮可停止。
- 仍未实现 root、通知监听、批量工作流；尚未测量真机操作速度。以下旧交付记录不代表本次版本。

# 待办：Agent 配置刷新与按需查询权限（本轮不实现）

- 用户已确认：更新 Agent 的全部配置后，已有会话从下一轮发送或重新生成开始生效；不要求中途改变正在执行的回复，也不要求新建会话或重启 App。
    - 当前已定位：ChatViewModel 初始化时缓存 `_agentProfile`；返回页面虽刷新 `agents`，未同步实际传入 AgentEngine 的配置。AgentEngine 在一次生成开始时固定工具白名单。
    - 后续在每轮执行入口读取最新绑定 Agent 配置，并检查工具、系统提示、模型等配置的完整应用链路；保留会话明确设置的覆盖项语义，不只修工具列表。检查与已有 systemPromptSnapshot/动态环境上下文的兼容，避免旧快照遮蔽更新。
- 用户反馈：目前几乎每轮回复开头都会检查会话权限。改为仅在准备修改文件、执行命令或其他涉及权限的动作时按需检查，不在普通问答开头惯例调用权限查询工具。
    - 后续核查 get_session_state 工具说明及环境/系统提示中诱导每轮查询的措辞；此项是减少无必要的主动查询，不移除执行层的权限校验、审批或拒绝机制。
- 本轮只记录待办，未修改运行时代码、构建或重新安装；后续实现后再验证已有会话下一轮刷新与普通问答/权限动作两类行为。

# 最新停点：UAH 品牌与多搜索服务

- 正式名 Used AI Harness、简称 UAH；使用设计包原始图标，保留安装身份。搜索支持 SearXNG、SerpApi（Google/Bing/百度）、Brave、Tavily、Exa、Google Custom Search 存量接口；全局选择一个当前服务，各服务配置独立保存。
- 构建、lint 和 diff 检查通过；模拟器检查名称/图标/菜单/密钥界面/旧地址读取。新增第三方真实 API 调用待用户密钥验证，未运行 JVM 测试。
- 已覆盖安装手机与模拟器。最新 APK SHA256 `4E4BF19CCC5F7D0CBDCD5D3E754115899D9597F682FC60A5D1D34C3601137030`；下文摘要均为历史版本。
- 详情与接口来源：`memory/2026-09-24-uah-search-providers.md`。品牌与搜索功能纳入本次检查点提交，前序检查点为 `d4a987c`；顶部两项待办仅记录，未实现。

# 最新停点：SearXNG 与 fetch/search

- 前序检查点提交 `d4a987c`；其后实现全局 SearXNG 配置、抽屉入口、fetch/search 和 Agent 禁用状态，当前新功能尚未提交。
- 构建与 lint 通过；模拟器模拟接口完成保存、search→fetch、禁用声明与伪造调用拒绝检查。真实用户实例待填写并测试。详情见 `memory/2026-09-24-web-search.md`。
- 当前 APK SHA256 `8F085A2928CF180675E7A577EBCEDECE6C4306F68C5569041C9C5B6C479751DA`。后文 APK 摘要均为历史版本。

# 最新补充：模型恢复与弹层手势

- 恢复接口能力同时清除上下文长度覆盖及输入草稿；仅上下文覆盖也可恢复。
- 统一弹层内容手势边界，处理列表边缘和长按后拖动误关闭；保留把手、关闭按钮及列表点选。
- 模拟器完成 700/1300ms 长按后拖动、普通列表滚动、上下文恢复保存检查；构建与 lint 通过。详见 `memory/2026-09-24-sheet-gestures-and-model-reset.md`。
- 本次 APK SHA256 `418EEFB202D19346E1D8A80F5FD226FE67D67B5222EABFCDD1FC89D72CD1190B`；后文 APK 摘要为前序版本。

# AgentApp 交接 · 2026-09-24

## 最新停点：回复操作、分支与权限上下文

- 最终交付：160 项针对性 JVM 测试、`assembleDebug`、`lintDebug` 通过（0 errors、39 warnings、5 hints）。APK SHA256 `3F4C68512B2D1F77BDE3132F879301AC6CD0C5AEF4018E89373DF1B6D2A527A5`，已覆盖安装 NX809J `912606610730` 与 `emulator-5554`，均 Success，未操作手机 UI。未提交或推送。全套旧符号链接问题仍未处理；本次文件 diff 检查通过，用户已有 `SessionScreen.kt` 尾部空白保留。

- 追加修复模型目录能力：真实本地 5580 的 `/models` 与 `/v1/models` 都返回 24 份丰富声明，旧客户端只接入了四类输入能力。现在持久化完整元数据，详情展示容量/思考/工具/温度/缓存及原始 JSON；上下文容量与思考档位参与会话，Anthropic 默认输出上限受声明约束。真实接口获取/保存/重开及聊天容量已在模拟器验证，见 `memory/2026-09-24-model-catalog-metadata.md`。手机安装后需重新获取列表并保存。

- 已根据手机日志确认文本错位来自聊天中的 `~` 被当作下标。改为仅数字简写上下标，保留显式 `<sub>/<sup>`。
- 回复操作按整次用户输入归并，生成结束并完成末尾淡入后显示；支持整组复制/编辑/删除与最新回复重新生成。所有用户消息和完整回复均支持分支；用户消息分支的正文/附件进入输入框，历史截止上一轮模型回答，不自动发送。
- 增加只读实时状态工具 `get_session_state`；历史修改后让旧环境快照失效并重新注入当前权限，不恢复历史权限。Plan 文案明确必须保存文件并调用 `exit_plan_mode` 提交审批。尚未复现手机权限持久化字段回退，亦不保证真实模型百分百主动提交。
- 118 项针对性测试、构建与 lint 通过。模拟器完成多轮工具操作栏、重新生成、两类分支和整组操作检查，详见 `memory/2026-09-24-reply-actions-and-permissions.md`。原始私有诊断文件和截图位于忽略的 `build/reply-fixes-20260924/`。
- 以下缓冲播放记录为本轮前一阶段；旧 APK 摘要不代表最新回复修复版本。

## 当前停点：延迟缓冲与分批淡入已实现

- 本轮按用户要求复用 Herdr 中现有 agy CLI，root 负责接入与模拟器验收。实现首次/耗尽后 200ms 缓冲、按输入速率分批播放、新增原生文字 220ms 淡入、自然结束快速收尾、取消立即同步。动画不逐帧重解析 Markdown，详细约定见 `memory/2026-09-24-stream-buffer-reveal.md`。
- 用户观察中反馈刷字太快，因此最终发布间隔为约 90/110/140ms，解析昂贵时最多 180ms；目标文字余量约 320ms。普通正文、标题、引用、列表、表格和代码支持新增文字淡入，公式/图表 WebView 不逐字淡入。
- Android Studio Pixel_10_Pro_XL / emulator-5554 / API 37，1080×2400、480dpi：完成 40/70/120/300 四档及 40/300 两档停顿恢复，六轮保存正文均与 1570 字符样例完全一致。速度为固定规则的模拟 token 单位，不是供应商真实分词结果。
- 四档单次 gfxinfo 卡顿帧分别为 0.21% / 0.10% / 0.74% / 1.98%，不能跨不同输入速率直接比较或当作真机性能结论。原始数据与截图在 `build/stream-buffer-20260924/final-*`；早期 `rate-40`、`fade-rate-40` 未正确识别速度标记，不用作分档证据。
- 上翻停止跟随、回到底部继续跟随、后台返回与主动停止已在模拟器检查。停止时原有保存清理路径会把协程取消当保存失败，现已让 IO 返回与 UI 状态收尾处于同一 NonCancellable 范围，复测不再出现错误横幅。
- 最终 `assembleDebug`、34 项相关 JVM 测试、`lintDebug` 通过；lint 0 errors、39 warnings、5 hints。全套测试另有 3 项未改动的 Windows 符号链接边界测试失败（以前环境跳过），未修复且不能宣称全套通过。
- APK：`app/build/outputs/apk/debug/app-debug.apk`，SHA256 `6976B5470E8AA7B4C281E2F041F0080D08BAD71B34C23304B29D4100E4AEA600`。模拟器已覆盖安装；按项目既有交付授权，NX809J（912606610730）也已覆盖安装并返回 Success，未操作手机 UI。
- 模拟器保留本轮 `Stream Buffer QA / stream-fixture`，loopback 服务 5582，便于用户继续输入 `rate-40` / `rate-70` / `rate-120` / `rate-300` / `pause-40` / `pause-300`。不要把此临时 Provider 当作日常备份；原 config/defaults 保存在被忽略的 `build/stream-buffer-20260924/`，可能含凭据，禁止提交。
- 项目开始时已有 `SessionScreen.kt` 用户改动，仍保留。未提交或推送。后续重点是用户对真实模型的阅读观感反馈；没有声称所有长文性能问题已解决。

## 2026-09-23 历史停点（以下为旧版记录）

用户要求先留交接并提交项目。下一轮重点是流式输出平滑性；用户确认同时存在文字成批出现和跟随滚动顿挫。当前代码包含优化初版，但不能宣称问题已解决。最新提出的缓冲播放方案仅完成可行性研判，尚未实现。

## 本轮项目变更

- 会话面板使用独立路由并复用 ChatSessions，展示计划、子代理及保存快照产物；头像支持图片失败回退。
- 历史、Agents、工作区、Skills、记忆、供应商列表统一操作菜单、管理态及原型列表行；补齐导入导出入口。
- Markdown 修复行内代码几何与标题/引用间距，增加脚注、安全 HTML 子集、上下标、高亮及离线 KaTeX/Mermaid。保留原生普通正文和 CommonMark；不承诺所有平台私有语法。
- 思考滑块修正填充，详情点选立即应用并返回滑块，恢复过渡动画，移除返回按钮。
- 模型供应商编辑页有可折叠模型目录与模型子页；子页完成只保存编辑草稿，父页统一持久化。
- 模型能力解析支持多个服务的输入模态/能力字段，显式 false 优先；资料见 `test-fixtures/model-capability-sources.md`。
- 供应商 model 字段仅用于测试连接；列表显示名称、协议、模型数量，无默认标识或协议字母头像。会话、Agent、子代理不再使用测试模型兜底；新会话快照不可用时保持未选择。
- 新增针对性单测、完整 Markdown 样例、模拟器辅助脚本、验收记录及离线资源许可证。IDE 配置为既有暂存文件，一并保留提交。

## 流式初版已改内容

- `StreamingTextPacer.kt`：只对展示文本追赶，完成/取消/内容替换立即同步完整原文，大于 4096 字符积压直接同步；避免拆开 UTF-16 代理对及组合附加字符，未覆盖所有 ZWJ 字素簇。
- `MarkdownContent.kt`：后台解析、自适应 32–120ms 节奏、复用相等块对象，保留旧内容直到新解析完成。仍是全文解析，不能等同增量解析。
- `ChatScreen.kt`：底部跟随改为 100ms 线性滚动；用户手势打断滚动时保留监听，生命周期取消继续传播。
- 新增四项单测覆盖追赶、结束/替换/大块同步、Unicode 和引用定义回溯更新。

## 待实现与待验证

1. 用户提议：首段到达先缓冲一小会儿，再稳定释放字符；上游暂停时排空已收文本；耗尽后恢复接收则重新缓冲。研判可行。150–250ms 是建议起点，并非已确认的固定参数。
2. 显示速度根据输入速率及积压平缓调整，限制额外延迟；正常结束快速平滑收尾，取消应立即停止动画并显示已收到文本。当前初版没有该状态机。
3. 不要每帧重解析整篇 Markdown；先定位全文解析、代码高亮、行内代码几何、公式/图表 WebView 重载热点。引用定义可能回溯影响前文，不能直接按字符块独立解析。
4. 验证手势上翻停止跟随、返回底部恢复、最终内容/复制一致、取消及生命周期恢复；测试长文、代码、公式、脚注、突发块和上游停顿。
5. 当前初版提高解析频率，可能增加 CPU；没有可靠对照证据前不得宣称性能改善。模拟器单次 gfxinfo 也不能代表真机体验。

## 已有验证和环境

- 本次 `testDebugUnitTest assembleDebug lintDebug` 成功：257 项测试，254 通过、3 跳过、0 失败/错误；lint 0 errors、39 warnings、3 hints，与原基线一致。`git diff --check` 通过。
- 最新 APK：`app/build/outputs/apk/debug/app-debug.apk`，SHA256 `B434D7DA4E53B2818F751BC2A2B68251E2497866B7CFC3A0725B969959F713EC`。已覆盖安装 NX809J（912606610730），安装返回 Success；未操作手机测试。
- 模拟器 emulator-5554，API 37，360dp；手机 API 36。手机 UI 验收由用户执行，自动安装授权仍有效。
- `.Codex/tools/streaming-fixture-server.py` 在 loopback 5582 提供无密钥固定 SSE：每 120ms 输出 48 字符，20 段 Markdown。用 `adb -s emulator-5554 reverse tcp:5582 tcp:5582` 接入。
- 模拟器经 UI 创建独立 Stream-QA 供应商，地址 `http://127.0.0.1:5582/v1`，模型 `stream-fixture`。当前新会话配置仍可能选中该模型。后续恢复 Local-UI-QA 的 gpt-5.6-luna，再移除临时供应商；不要误把临时供应商导出为日常备份。
- 基线 gfxinfo：584 帧，66 janky（11.30%），p50/p90/p95/p99 为 22/40/46/65ms；初版后 335 帧、137 janky（40.90%），34/101/150/400ms。初版存在明显退化信号，下一轮优先复核并修正，不得视为性能改善。两次包含发送与布局开销，后一次采集较晚且期间有构建，未完成可比性核验及截图验收。原始数据在 verification/ui-v3-refinement/stream-*-gfx.txt。
- 本地真实模型服务 5580；含 Key 的导入备份仅在被忽略的 `.Codex/local/`。`Backup-EmulatorProviders.ps1` 可重新导出，禁止把密钥写入交接或提交。
- 原型服务 `http://127.0.0.1:8765/index.html`。下一轮先检查服务是否仍运行；8766 仓库根服务已停止，不要暴露本地凭据目录。
- 详细已验收/待实机项见 `test-fixtures/ui-v3-completion-acceptance.md` 与 `test-fixtures/ui-v3-refinement-acceptance.md`；其中旧 APK/测试计数是各批历史记录。

## 参考资料

- Vercel smoothStream：<https://ai-sdk.dev/docs/reference/ai-sdk-core/smooth-stream>，提供分块缓冲/定时释放思路，不是本项目实现或主流商业 App 内部机制的证据。
- Compose 阶段和状态读取：<https://developer.android.com/develop/ui/compose/phases>。

交接时不继续扩展功能，不推送或发布。
