# 会话体验第二版

- 用户授权构建完成后直接覆盖安装到无线连接设备；不代替用户进行手机 UI 测试。
- Markdown 使用 Markwon 4.6.2 原生 TextView，支持表格、任务列表、代码块，未启用远程图片/HTML 插件。
- 移除附件 20 MB、原生单件 4 MB、历史合计 12 MB 的字节限制。保留每条 8 个附件与模型能力校验。迁移包总量 64 MB 边界独立保留。
- read_file 对支持的图片/PDF 创建附件快照，在该批全部工具结果之后插入原生内容消息；originToolCallId 标记供 UI 隐藏内部消息。
- FileChange 记录 write_file 前后内容，成功写入后随工具结果保存；超大快照可省略差异预览，不限制写文件。聊天可查看本次 diff，文件查看页可查看最近修改。
- 用户附件位于文字气泡上方。子代理入口移到会话侧边抽屉，点击导航独立只读会话，父 ViewModel 留在导航栈继续运行。
- SubagentRegistry 精确索引子任务 Job，用户填写可选理由确认中止后，子任务取消转为包含理由的正常工具返回；父任务取消仍传播 CancellationException。
- 测试流程见 .Codex/test-fixtures/attachments/README.md；验证与安装结果待本轮收尾补充。

## 最终验证与交付

- JBR 21：assembleDebug、testDebugUnitTest、lintDebug 均成功。
- 单元测试：{'tests': 101, 'failures': 0, 'errors': 0, 'skipped': 0}。Lint：{'Warning': 33, 'Hint': 2}（0 error）。
- APK：app/build/outputs/apk/debug/app-debug.apk，23242775 字节；SHA-256：B3B26242E719D42C7F78847DBF22AD88C4D52B3E15B1003D9D5878086B0F3323。
- adb 指定一个 NX809J 无线连接执行 install -r，返回 Success；保留原有数据，未进行设备 UI 自动化或创建测试数据。
