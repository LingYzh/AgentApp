# UI v3 第四轮：正文细节、工具样本与会话栏

## 用户修订（覆盖原型旧约定）

- 顶部反馈悬浮在 topbar 下方，不挤占正文高度；CODEX_HANDOFF.md 的“反馈占位”要求已被这次用户指示覆盖。
- 会话栏上方是真实会话标题，下方是 Agent 名称；移除标题处的模型选择入口。输入框内的模型选择保留。
- 增加标题旁的手动改名入口。首次发送继续使用首条消息摘要作初始标题，不增加额外模型请求。生成/历史操作期间禁用改名，保存成功后才发布标题。

## 对齐原型

- 引用左侧直角、右侧圆角，3dp陶土色线；引用段落14sp和muted色。
- 行内代码使用独立边框、圆角与底色，保持整个段落的选择、复制、链接和换行。
- Markdown任务勾选改为原生绘制14dp圆角方框，不再使用Unicode字符；仅呈现，不改变文档。
- 工具记录对照原型26“工具记录·样本”：紧凑无外框工具行；Shell命令与输出合并；Diff头部统计与操作、单行号/符号/代码、底部高度控制。原始记录、退出码与缺少快照的边界保留。
- Shell 原文复制不混入显示用 `$ `；输出到达不重置换行/高度。执行中、待批准、排队与未知返回分别显示，当前后端没有实时stdout时不宣称持续输出。
- Diff按文件扩展名后台高亮，收起显示文件名与统计，展开将其移入面板头；修改前/后原文复制移到更多菜单。
- 代码高度仍受阅读预算限制（展开后520/560dp并可内部滚动），避免长输出撑开聊天页。

## 验证

- 最终 testDebugUnitTest / assembleDebug / lintDebug 全部通过。JVM 225项：222通过、3跳过、0失败；lint 0错误、39警告、4提示。
- diff-check通过。测试样本沿用并扩充 `.Codex/test-fixtures/ui-v3-markdown-and-directories.md`。
- 手机交互仍由用户验收；本轮不自动点击手机或生成实机测试数据。
- 无线ADB覆盖安装成功，设备lastUpdateTime `2026-09-22 22:38:15`，未清除数据。
- APK `app/build/outputs/apk/debug/app-debug.apk`，SHA256 `FECD51F85FA84B94E8220303F26FF3BAE0D9400A6124DA4586FDD1486E7AD2B3`。
